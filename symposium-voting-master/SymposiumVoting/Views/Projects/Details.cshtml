@model SymposiumVoting.Models.Project

@{
    ViewBag.Title = "Details";
}
@{int id = Model.ID;}

<h2>Details</h2>

<div>
    <h4>Project</h4>
    <p>

        @Html.ActionLink("Edit", "Edit", new { id = Model.ID }) |
        @Html.ActionLink("Back to Symposium", "Details", "Symposiums", new { id = Model.symposiumID }, new { })
    </p>
	<hr />

    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.CourseNumber)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CourseNumber)
        </dd>
        <dt>
            Students
        </dt>
        <dd>
            @Html.DisplayFor(model => model.students)
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Generate QR Code", "code_generation", new { id = Model.ID }, new { @class = "btn" })
    @Html.ActionLink("Vote For Project", "project_vote", new { id = Model.ID }, new { @class = "btn" })
</div>

